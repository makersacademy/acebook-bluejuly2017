<%= form_with(model: random_image, local: true) do |form| %>
  <% if random_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(random_image.errors.count, "error") %> prohibited this random_image from being saved:</h2>

      <ul>
      <% random_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
