<%= form_with(model: friend, local: true) do |form| %>



  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :body, id: :friend_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
